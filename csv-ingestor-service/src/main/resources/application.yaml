server:
  port: 8082

spring:
  application:
    name: csv-ingestor-service

  datasource:
    url: jdbc:postgresql://localhost:5432/ingestor
    username: ingestor_user
    password: secret2
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  liquibase:
    change-log: classpath:/db/changelog/app-changelog.yaml
    default-schema: public
---
spring:
  config:
    activate:
      on-profile: test
  liquibase:
    enabled: false
